# Go Load Balancer Demo

A simple load balancer implementation in Go that demonstrates how to rotate through a series of API keys for external service calls.

## Features

- Round-robin load balancing of API keys
- Simple HTTP server with JSON responses
- Multiple endpoints to demonstrate the load balancer in action